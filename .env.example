# 환경 변수 설정 (.env)
# 실제 운영 환경에서는 이 파일을 .gitignore에 추가하고, 
# 시크릿 관리 시스템을 사용하는 것이 좋습니다.

# 애플리케이션 설정
NODE_ENV=development
PORT=3000

# 데이터베이스 연결 설정
DATABASE_URL=postgresql://username:password@localhost:5432/f1_db?schema=public

# Redis 연결 설정
REDIS_URL=redis://localhost:6379

# 인증 및 보안 설정 (필요한 경우)
# JWT_SECRET=your_jwt_secret_here
# JWT_EXPIRATION=3600

# 파이썬 환경 설정 (데이터 수집용)
PYTHONPATH=./src/ingest/python
